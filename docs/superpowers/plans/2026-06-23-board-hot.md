# 게시판 인기글(HOT) 실데이터화 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `/board` 인기글(HOT) 섹션을 mock(`CM_HOT`)에서 기존 게시글 집계 기반 실데이터로 전환한다.

**Architecture:** `getBoardListData`가 이미 가져오는 게시글 전체(`rows`)를 재사용해 참여 점수 상위 2개를 인기글로 파생한다. lastReply만 선정 글의 최신 댓글을 1회 추가 조회한다. 변환은 클라이언트 안전 순수 유틸(`lib/board.ts`)에 두고, 컴포넌트는 데이터 출처만 prop으로 교체(디자인 마크업 보존). 태그·말씀은 정적 유지.

**Tech Stack:** Next.js 16 App Router, TypeScript strict, supabase-js + RLS.

---

## File Structure

- `src/lib/board-data.ts` (수정) — `HotThread`에서 `reactions` 제거·`lastReply?` 선택화, `CM_HOT` 삭제
- `src/lib/board.ts` (수정) — `HotRow` 타입·`toHotThreadView` 변환기 추가
- `src/server/services/board.ts` (수정) — `BoardListData.hot` 추가, `getBoardListData`에서 집계·최신 댓글 조회
- `src/app/board/page.tsx` (수정) — `hot` prop 전달
- `src/app/board/_components/desktop/BoardDesktop.tsx` (수정) — `hot` 받아 `HotSection`에 전달
- `src/app/board/_components/desktop/HotSection.tsx` (수정) — `CM_HOT` 제거 → `hot` prop
- `src/app/board/_components/mobile/BoardMobile.tsx` (수정) — `CM_HOT` 제거 → `hot` prop

---

## Task 1: 타입 정리 (board-data.ts)

**Files:**
- Modify: `src/lib/board-data.ts`

- [ ] **Step 1: `HotThread` 타입 수정**

`HotThread` 타입에서 `reactions` 줄을 제거하고 `lastReply`를 선택으로 변경한다. 수정 후 모습:

```ts
export type HotThread = {
  id: string;
  cat: Exclude<BoardCategoryKo, "전체">;
  heat: number;
  title: string;
  excerpt: string;
  author: string;
  church: string;
  avatar: string;
  date: string;
  comments: number;
  likes: number;
  views: number;
  lastReply?: { name: string; text: string; when: string };
};
```

- [ ] **Step 2: `CM_HOT` 상수 삭제**

`export const CM_HOT: ReadonlyArray<HotThread> = [ ... ];` 블록 전체(현재 49~92행)를 삭제한다. `CM_TAGS`·`CM_VERSE`·`BOARD_SORTS`·기타 타입은 그대로 둔다.

- [ ] **Step 3: 타입 확인**

Run: `pnpm exec tsc --noEmit`
Expected: `CM_HOT` 참조가 남은 곳(HotSection·BoardMobile)에서 에러 발생 — Task 4·5에서 해소. 이 시점에선 에러가 예상되므로 다음 단계로 진행한다.

> 참고: 이 Task는 단독 커밋하지 않고 Task 2~5와 함께 한 번에 커밋한다(중간 상태가 컴파일되지 않으므로).

---

## Task 2: 변환기 추가 (board.ts)

**Files:**
- Modify: `src/lib/board.ts`

- [ ] **Step 1: import에 `HotThread` 타입 추가**

`src/lib/board.ts` 상단 타입 import 목록(`./board-data`)에 `HotThread`를 추가한다:

```ts
import type {
  FeedPost,
  BoardCategoryKo,
  BoardCategoryEn,
  BoardFeedKind,
  BoardSort,
  HotThread,
} from "./board-data";
```

- [ ] **Step 2: `HotRow` 타입·`toHotThreadView` 변환기 추가**

`sortFeedPosts` 함수 아래에 append:

```ts
// 인기글 파생용 평면 행
export type HotRow = {
  id: string;
  category: string | null;
  title: string;
  excerpt: string | null;
  viewCount: number;
  createdAt: Date;
  authorName: string | null;
  authorChurch: string | null;
  commentCount: number;
  likeCount: number;
};

// 참여 점수 가중치
export const HOT_WEIGHT = { comment: 10, like: 5 } as const;

export function hotScore(row: {
  viewCount: number;
  commentCount: number;
  likeCount: number;
}): number {
  return (
    row.viewCount +
    row.commentCount * HOT_WEIGHT.comment +
    row.likeCount * HOT_WEIGHT.like
  );
}

// 평면 행 → 인기글 뷰모델. heat는 최고 점수 대비 0~100 정규화.
export function toHotThreadView(
  row: HotRow,
  score: number,
  maxScore: number,
  lastReply?: { name: string; text: string; when: string },
): HotThread {
  const cat = (row.category && row.category in CATEGORY_KIND ? row.category : "나눔") as Cat;
  const name = row.authorName ?? "익명";
  return {
    id: row.id,
    cat,
    heat: maxScore > 0 ? Math.round((score / maxScore) * 100) : 0,
    title: row.title,
    excerpt: row.excerpt ?? "",
    author: name,
    church: row.authorChurch ?? "",
    avatar: name.slice(0, 1),
    date: formatDate(row.createdAt),
    comments: row.commentCount,
    likes: row.likeCount,
    views: row.viewCount,
    ...(lastReply ? { lastReply } : {}),
  };
}
```

> `Cat`·`CATEGORY_KIND`·`formatDate`는 이미 같은 파일에 정의/임포트되어 있어 그대로 참조한다.

---

## Task 3: 서비스 집계 (board.ts service)

**Files:**
- Modify: `src/server/services/board.ts`

- [ ] **Step 1: import 보강**

상단 `@/lib/board` import에 `toHotThreadView`·`hotScore`·`HotRow`를, `@/lib/board-data` import에 `HotThread`를 추가한다:

```ts
import {
  toFeedPostView,
  categoryToKind,
  toHotThreadView,
  hotScore,
  BOARD_CATEGORIES_KO,
  BOARD_CATEGORY_EN,
  type HotRow,
} from "@/lib/board";
import type {
  FeedPost,
  BoardCategory,
  ActiveMember,
  BoardStats,
  HotThread,
} from "@/lib/board-data";
```

- [ ] **Step 2: `BoardListData`에 `hot` 추가**

```ts
export type BoardListData = {
  posts: FeedPost[];
  categories: BoardCategory[];
  members: ActiveMember[];
  stats: BoardStats;
  hot: HotThread[];
};
```

- [ ] **Step 3: 집계·최신 댓글 조회 후 `hot` 산출**

`getBoardListData`의 `return { posts: list, categories, members, stats };` 바로 앞에 삽입하고, return 문에 `hot`을 추가한다:

```ts
  // 인기글 — 참여 점수 상위 2개(rows 재사용). 동점은 최신순(rows가 created_at desc).
  const scored = rows.map((r) => ({
    r,
    score: hotScore({
      viewCount: r.view_count,
      commentCount: r.comments[0]?.count ?? 0,
      likeCount: r.post_likes[0]?.count ?? 0,
    }),
  }));
  const topHot = [...scored].sort((a, b) => b.score - a.score).slice(0, 2);
  const maxScore = topHot[0]?.score ?? 0;

  // 선정 글의 최신 댓글 1건씩 (작성자 임베드, 최신순 조회 후 글별 첫 건)
  const hotIds = topHot.map((h) => h.r.id);
  const lastReplyByPost = new Map<
    string,
    { name: string; text: string; when: string }
  >();
  if (hotIds.length > 0) {
    const { data: cms } = await supabase
      .from("comments")
      .select("post_id, body, created_at, author:profiles(name)")
      .in("post_id", hotIds)
      .order("created_at", { ascending: false });
    for (const c of cms ?? []) {
      if (lastReplyByPost.has(c.post_id)) continue; // 이미 최신 1건 확보
      const ca = one(c.author);
      lastReplyByPost.set(c.post_id, {
        name: ca?.name ?? "익명",
        text: c.body,
        when: formatDate(new Date(c.created_at)),
      });
    }
  }

  const hot: HotThread[] = topHot.map(({ r, score }) => {
    const author = one(r.author);
    const hotRow: HotRow = {
      id: r.id,
      category: r.category,
      title: r.title,
      excerpt: r.excerpt,
      viewCount: r.view_count,
      createdAt: new Date(r.created_at),
      authorName: author?.name ?? null,
      authorChurch: author?.church ?? null,
      commentCount: r.comments[0]?.count ?? 0,
      likeCount: r.post_likes[0]?.count ?? 0,
    };
    return toHotThreadView(hotRow, score, maxScore, lastReplyByPost.get(r.id));
  });

  return { posts: list, categories, members, stats, hot };
```

> `one()`은 이 파일에 이미 정의되어 있어 그대로 쓴다. `formatDate`도 이미 import됨.

---

## Task 4: 데스크톱 배선 (page·BoardDesktop·HotSection)

**Files:**
- Modify: `src/app/board/page.tsx`
- Modify: `src/app/board/_components/desktop/BoardDesktop.tsx`
- Modify: `src/app/board/_components/desktop/HotSection.tsx`

- [ ] **Step 1: page.tsx에서 `hot` 전달**

데스크톱 분기의 `<BoardDesktop>`에 `hot={data.hot}` 추가, 모바일 분기의 `<BoardMobile>`에 `hot={data.hot}` 추가:

```tsx
        <BoardDesktop
          posts={data.posts}
          categories={data.categories}
          members={data.members}
          stats={data.stats}
          hot={data.hot}
        />
```
```tsx
    <BoardMobile
      deviceType={device}
      posts={data.posts}
      categories={data.categories}
      stats={data.stats}
      hot={data.hot}
    />
```

- [ ] **Step 2: BoardDesktop에서 `hot` 받아 전달**

import 타입에 `HotThread` 추가:

```tsx
import type {
  BoardCategory,
  FeedPost,
  ActiveMember,
  BoardStats,
  HotThread,
} from "@/lib/board-data";
```

props 구조분해와 타입에 `hot` 추가:

```tsx
export default function BoardDesktop({
  posts,
  categories,
  members,
  stats,
  hot,
}: {
  posts: FeedPost[];
  categories: BoardCategory[];
  members: ActiveMember[];
  stats: BoardStats;
  hot: HotThread[];
}) {
```

`<HotSection palette={palette} />`를 `<HotSection palette={palette} hot={hot} />`로 변경.

- [ ] **Step 3: HotSection에서 prop 사용**

`import { CM_HOT } from "@/lib/board-data";`를 타입 import로 교체하고 prop을 받는다:

```tsx
import type { Palette } from "@/app/_components/shared/palette";
import type { HotThread } from "@/lib/board-data";
import HotThreadCard from "./HotThreadCard";

type Props = { palette: Palette; hot: HotThread[] };

export default function HotSection({ palette, hot }: Props) {
```

본문의 `{CM_HOT.map((p) => (` 를 `{hot.map((p) => (` 로 변경.

---

## Task 5: 모바일 배선 (BoardMobile)

**Files:**
- Modify: `src/app/board/_components/mobile/BoardMobile.tsx`

- [ ] **Step 1: import에서 `CM_HOT` 제거, `HotThread` 타입 추가**

`@/lib/board-data` import 블록을 다음으로 교체(`CM_VERSE`·`BOARD_SORTS`는 유지):

```tsx
import {
  BOARD_SORTS,
  CM_VERSE,
  type BoardCategory,
  type BoardSort,
  type BoardStat,
  type BoardStats,
  type FeedPost,
  type HotThread,
} from "@/lib/board-data";
```

- [ ] **Step 2: Props·구조분해에 `hot` 추가**

```tsx
type Props = {
  deviceType?: "ios" | "android";
  posts: FeedPost[];
  categories: BoardCategory[];
  stats: BoardStats;
  hot: HotThread[];
};
```
```tsx
export default function BoardMobile({
  deviceType = "ios",
  posts,
  categories,
  stats,
  hot,
}: Props) {
```

- [ ] **Step 3: 렌더에서 prop 사용**

인기글 렌더의 `{CM_HOT.map((p) => (`를 `{hot.map((p) => (`로 변경.

---

## Task 6: 검증·커밋

**Files:** 없음(검증)

- [ ] **Step 1: 타입·lint·build**

Run: `pnpm exec tsc --noEmit && pnpm lint && pnpm build`
Expected: 모두 통과(에러 0). `CM_HOT` 미참조 확인.

- [ ] **Step 2: `CM_HOT` 잔존 참조 없음 확인**

Run: `grep -rn "CM_HOT" src`
Expected: 출력 없음.

- [ ] **Step 3: 코드 변경 커밋**

```bash
git add src/lib/board-data.ts src/lib/board.ts src/server/services/board.ts "src/app/board/page.tsx" "src/app/board/_components/desktop/BoardDesktop.tsx" "src/app/board/_components/desktop/HotSection.tsx" "src/app/board/_components/mobile/BoardMobile.tsx"
git commit -m "feat: 게시판 인기글 실데이터화"
```

---

## Task 7: 로컬 e2e 검증·문서 커밋

**전제:** 로컬 Supabase가 127.0.0.1인지 먼저 확인(`npx supabase status`). 운영 DB 금지. 계정·콘텐츠 없으면 `pnpm seed`.

- [ ] **Step 1: 데스크톱 확인**

`pnpm dev` 후 admin/member 로그인(proxy가 /board 회원 가드) → `/board` 데스크톱에서 인기글 2개가 실제 시드 글로 표시, heat 게이지·카테고리·작성자·교회·날짜·댓글/좋아요/조회수 정상.

- [ ] **Step 2: lastReply 확인**

댓글이 있는 글이 인기글에 포함되면 답글 영역(작성자·내용·시간)이 최신 댓글로 표시되는지, 댓글 없는 글은 답글 영역이 생략되는지 확인.

- [ ] **Step 3: 모바일 확인**

모바일 뷰에서 인기글 가로 카드(heat·카테고리·제목·요약·댓글/좋아요/조회) 정상.

- [ ] **Step 4: 태그·말씀 회귀 확인**

트렌딩 태그(`CM_TAGS`)·오늘의 말씀(`CM_VERSE`)이 기존과 동일하게 표시되는지 확인(정적 유지).

- [ ] **Step 5: plan 문서 커밋**

```bash
git add docs/superpowers/plans/2026-06-23-board-hot.md
git commit -m "docs: 게시판 인기글 실데이터화 실행 plan"
```

---

## Self-Review 결과

- **스펙 커버리지:** 타입 정리(Task 1)·변환기(Task 2)·서비스 집계+lastReply(Task 3)·데스크톱 배선(Task 4)·모바일 배선(Task 5)·검증(Task 6)·e2e(Task 7) — 스펙 전 항목 매핑됨. 태그·말씀 정적 유지(비범위) 준수.
- **타입 일관성:** `HotRow`·`hotScore`·`toHotThreadView`(board.ts) ↔ 서비스 사용부 일치, `BoardListData.hot: HotThread[]` ↔ page/BoardDesktop/HotSection/BoardMobile prop 타입 일치, `HotThread`에서 `reactions` 제거·`lastReply?` 선택화가 카드의 `lastReply &&` 가드와 정합.
- **마이그레이션 없음·디자인 보존:** 마크업/클래스 변경 없이 데이터 출처만 교체.
- **중간 컴파일:** Task 1 단독은 컴파일 실패가 예상되므로 Task 1~5를 묶어 Task 6에서 단일 커밋(주의 명시).
